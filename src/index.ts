/**
 * src/index.ts — Application Entry Point
 *
 * PURPOSE:
 *   Bootstraps the Incident Response Agent application.
 *   This is a minimal stub for Module 1 (project bootstrap).
 *   Full initialisation logic (config loading, Mastra setup, Express server)
 *   will be added in subsequent modules.
 *
 * EXECUTION:
 *   Development : npm run dev    (tsx watch — hot-reload)
 *   Production  : npm start      (node dist/index.js after npm run build)
 */

// Module 1 — intentional stub
// Subsequent modules will import and initialise:
//   - Environment configuration (src/config/env.ts)
//   - OpenTelemetry tracer     (src/config/telemetry.ts)
//   - Pino logger              (src/utils/logger.ts)
//   - Qdrant client            (src/services/qdrant/)
//   - Mastra instance          (src/mastra/)
//   - Express application      (src/api/)

console.log("🚨 Incident Response & Post-Mortem Agent — starting up");
console.log("   Module 1 bootstrap complete. Awaiting Module 2.");
